export enum CONDITION_OPERATOR {
	IS_EMPTY = 1,
	IS_NOT_EMPTY,

	IS_BETWEEN,
	IS_NOT_BETWEEN,

	IS_EXACTLY,
	IS_NOT_EXACTLY,

	IS_BEFORE,
	IS_AFTER,

	CONTAINS,
	DOES_NOT_CONTAINS,
	STARTS_WITH,
	ENDS_WITH,

	WORD_COUNT_GREATER_THAN,
	WORD_COUNT_GREATER_THAN_OR_EQUAL,
	WORD_COUNT_LESS_THAN,
	WORD_COUNT_LESS_THAN_OR_EQUAL,
	WORD_COUNT_EQUAL,

	IN,
	NOT_IN,

	GREATER_THAN,
	GREATER_THAN_OR_EQUAL,
	LESS_THAN,
	LESS_THAN_OR_EQUAL,

	COMPARE_TODAY,

	CURRENT_VIEWER,
}

export enum DATE_OPERATOR {
	TODAY = 1,
	YESTERDAY,
	TOMORROW,

	THIS_WEEK,
	THIS_MONTH,
	THIS_YEAR,

	LAST_WEEK,
	LAST_MONTH,
	LAST_YEAR,

	NEXT_WEEK,
	NEXT_MONTH,
	NEXT_YEAR,

	CUSTOM_DATE,

	CUSTOM_FIELD,
}

export enum PERIOD_PREFIX {
	THIS = 1,
	NEXT = 2,
	LAST = 3,
}

export enum PERIOD {
	DAY = 1,
	WEEK = 2,
	MONTH = 3,
	YEAR = 4,
}

export enum FILTER_VALUE_TYPE {
	CUSTOM = 1,
	OTHER_FIELD = 2,
	OTHER_BOARD = 3,
}

export enum LOGICAL_OPERATOR {
	AND = 1,
	OR = 2,
	CUSTOM = 3,
}

export enum FORMULA_FILTER_TYPE {
	NUMBER = 1,
	TEXT = 2,
	DATE = 3,
}
